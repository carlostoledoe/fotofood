<%= form_with(model: board) do |form| %>
  <% if board.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
        <% board.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, "Título del tablero:", style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <% form.label :user_id %>
    <%= form.hidden_field :user_id, value: @user %>
  </div>

  <div>

    <%= form.label :post_id, "Elige un post:", style: "display: block", class: "mt-3"  %>
    <%= form.collection_select :post_id, @tus_post, :id, :title  %>
  </div>

  <div>
    <%= form.submit "Crear", class: "btn btn-warning mt-4 btn-lg" %>
  </div>
<% end %>

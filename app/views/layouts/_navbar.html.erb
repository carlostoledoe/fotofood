<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: black;">
  <div class="container-fluid">
    <a class="navbar-brand colorbrand" href="/">Fotofood.cl</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Inicio</a>
        </li>
        <li class="nav-item">
          <%= link_to "Nueva fotofood", new_post_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link 2</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link 3</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link 4</a>
        </li>
      </ul>
      <div>
        <% if user_signed_in? %>
          <%= button_to( "Salir", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-sm" ) %>
        <% else %>
          <%= link_to "Regístrate", new_user_registration_path, class: "btn btn-warning btn-sm" %>
          <%= link_to "Ingresar", new_user_session_path, class: "btn btn-outline-primary btn-sm"  %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
  <form class="d-flex">
    <%= form_with url: posts_path, method: :get do |f| %>
      <%= f.text_field :search, :class => "form-control me-2", placeholder: "Buscar fotofood" %>
      <%= f.submit "Search", :class => "btn btn-outline-warning" %>
    <% end %> 
  </form>
<%= image_tag "banner.gif", class: "w-100 mt-3" %>
<% if user_signed_in? %>
  <span class="lead colorbrand" >Bienvenido: <%= current_user.email %></span>
<% end %>